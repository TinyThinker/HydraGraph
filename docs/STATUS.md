# Project Status

> Auto-generated by /sync-docs. Edit ROADMAP.md or CHANGELOG.md as the source of truth.
> Last synced: 2026-09-02 (seed — first generation done by hand as part of the docs reorg)

---

## Version

Current: v0.3.0
Last commit: ca212a5 — 2026-09-01 02:21:09 -0700
Last commit message: docs: revise MVP roadmap around a competitive scan

---

## Phase Checklist

### Current State — Active blockers
- [ ] Six built capabilities are unreachable from the UI (retry / cancel / edit / per-node model / per-node persona) — the subway-pill refactor re-implemented only delete
- [ ] Settings split-brain — the "no provider configured" banner survives adding an API key until reload

### Phase 1 — Stop the bleeding (days 1–4)
- [ ] Consolidate on `useSettingsStore`; delete the duplicate settings state in `useTreeStore`
- [ ] Retry on errored nodes; regenerate on idle ones
- [ ] Cancel while streaming
- [ ] Delete the unused `@dagrejs/dagre` dependency; wire or remove `openRouterBaseUrl`

### Phase 2 — Build the demo (days 5–9)
- [ ] Per-node model picker and per-node persona, rehomed into the reader panel
- [ ] Fan-out: one prompt → N branches, different model/persona each, parallel from identical ancestry
- [ ] Compare view: selected siblings column by column
- [ ] Model pricing table → per-node / per-tree / counterfactual cost in dollars

### Phase 3 — The front door (days 10–12)
- [ ] Demo tree shipped with the app: canned responses, no key required
- [ ] Static deploy; landing page is the app with the demo preloaded
- [ ] Key setup in three steps with a live connection test
- [ ] One honest line about where data lives, plus an export nudge

### Phase 4 — Post it, then listen (days 13–14)
- [ ] Ship to local-model communities, BYOK power users, prompt engineers
- [ ] Lead with the comparison capture
- [ ] Reply to every person; ship one fix the same day
- [ ] Track who came back a second time, and what they did

### Later — Open track (gated on Phase 4 evidence)
- [ ] Track A — Deepen arbitration
- [ ] Track B — Close the loop
- [ ] Track C — Trust and reach

---

## Active Blockers

1. Six built capabilities are unreachable from the UI — `submitPrompt` / `cancelGeneration`
   are only called from the unmounted `PromptSection.tsx`. An errored node is a dead end.
2. Settings split-brain — `useTreeStore.settings` and `useSettingsStore.settings` are two
   copies of one row; the modal writes only one, so first-run friction persists.

---

## Next Actions (priority order)

1. [Phase 1 / Current State] Consolidate on `useSettingsStore`; delete the duplicate settings state in `useTreeStore` — fixes the first-run banner
2. [Phase 1] Retry on errored nodes; regenerate on idle ones (rewire `submitPrompt`)
3. [Phase 1] Cancel while streaming (rewire `cancelGeneration`)
4. [Phase 1] Delete the unused `@dagrejs/dagre` dependency; wire or remove `openRouterBaseUrl`
5. [Phase 2] Per-node model picker and per-node persona, rehomed into the reader panel

---

## Key Constraints (always in force)

- Store mutating actions MUST be immutable — new object + new container references, or
  React Flow re-render locks (`CLAUDE.md`; `ARCHITECTURE.md` §7.2).
- In-flight streaming text lives in `useTreeStore.liveText` (keyed by node id), NOT on
  the node record — a token delta must never produce a new `TurnNode` object
  (`ARCHITECTURE.md` §5.3).
- Ancestry traversal reads the in-memory Zustand node map, never IndexedDB; the only DB
  read on the submit path is the tree record (`ARCHITECTURE.md` §3.1, §5.1).
- Canvas node positions are a pure function of tree structure — no manual placement to
  preserve; dragging and resizing are disabled (`ARCHITECTURE.md` §5.5).
- Max 150 lines per component file; separate UI rendering from store logic (`CLAUDE.md`).
- DB schema is at version 4; every bump only backfills new optional fields
  (`ARCHITECTURE.md` §3.4).

---

## Last Built (v0.3.0 — 2026-09-01)

Dual-pane workspace & subway canvas (the `poc_enhancements_1` track): the UI was
reshaped from a single-pane full-card canvas into canvas-for-wayfinding +
chat-pane-for-reading. Dedicated settings store with layered dispatch resolution;
`SplitLayout`; `ChatStreamView` / `ChatInputBar` with `forkAndSubmit`; two-way
selection sync; deterministic `d3-hierarchy` auto-layout; 240×72 station pills;
active-path highlighting. Verified on `poc_enhancements_1` (2026-09-01): typecheck
clean, lint clean, 253 tests across 32 files passing.
