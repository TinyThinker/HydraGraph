// Shared canvas node dimensions. Manual resize is gone (Task 3.1); every node
// renders at this fixed "station pill" size and the deterministic layout
// (Task 3.2) spaces nodes using these constants.
export const NODE_WIDTH = 240
export const NODE_HEIGHT = 72
